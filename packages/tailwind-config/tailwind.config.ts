import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {},
  },
  darkMode: "class",
  content: [],
  plugins: [],
};

export type TailwindConfig = Config;

export default config;
